# from models.goods_info import GoodsInfo
# from models.pop_types_info import POPTypesInfo
from utils import Utils

#TODO 先不用管，到时候生成树函数及表格导出函数写完了再想他
if __name__ == '__main__':
    goods_info = Utils. _create_goods_info()
    print(goods_info)
    # a = PMInfo()
    # print(a.pop_info_dict)